# Handoff — TASK-009 (próxima da fila; TASK-006/007/008 concluídas)

Última ferramenta: Claude Code
Última atualização: 2026-08-29

## Objetivo
Concluir ADR-002 (redesign das telas de diagrama do ClassMap para bater com o artefato-protótipo validado). Faltam 2 das 5 tasks: **TASK-009** (Visão do Sistema) e **TASK-010** (modais de import/export). Este handoff é o ponto de entrada para retomar em **TASK-009**.

## Estado atual
Nenhuma task está em `.agents/tasks/active/` neste momento — TASK-006, TASK-007 e TASK-008 foram concluídas nesta sessão e já estão em `.agents/tasks/completed/`. Nada está pela metade: build/lint/test estão limpos, sem página de preview temporária sobrando (`main.tsx` está igual ao HEAD). **Nada foi commitado ainda** — todo o trabalho das 3 tasks está só na working tree (`git status`/`git diff` mostram as mudanças reais).

## Concluído
- **TASK-006** — design system + shell de 3 colunas (`src/features/diagram-shell/`: `DiagramShell`, `Toast`/`useToast`, tokens reais do artefato em `src/index.css`). Ver `.agents/tasks/completed/TASK-006-design-system-shell-diagramas.md`.
- **TASK-007** — Diagrama de Classes: canvas avançado (zoom/pan, modo de conexão, busca+stats, inspector fixo). Criou `diagram-shell/canvasTransform.ts`+`useCanvasZoomPan.ts` (compartilhados), `class-diagram/connectMode.ts`, `EdgeTypeGrid.tsx`. `DiagramEditorPage` não usa mais `AppLayout` (full-bleed). Ver `.agents/tasks/completed/TASK-007-canvas-avancado-diagrama-classes.md`.
- **TASK-008** — Diagrama de Objetos: mesmo shell/zoom-pan da TASK-007, paleta `--object-accent`, `ClassPickerModal.tsx` (modal de criação, substituiu o formulário de 2 selects). `ObjectDiagramPage` também não usa mais `AppLayout`. Ver `.agents/tasks/completed/TASK-008-canvas-avancado-diagrama-objetos.md`.
- `CONTEXT.md` e `docs/architecture/components.md` atualizados refletindo as 3 tasks.
- Validação de cada task: `npm run build`/`npm run lint`/`npm test` limpos (82 testes passando no total) + comparação visual manual no navegador embutido (dark/light) contra o artefato (`https://claude.ai/code/artifact/4f3aa122-e526-4577-8d62-b4bf916453dc`), via uma página de preview temporária (`__preview__.tsx` + toggle em `main.tsx`) sempre revertida antes de terminar a task.

## Em andamento
Nada — ponto de parada limpo entre tasks.

## Próximos passos
1. Ler `.agents/decisions/ADR-002-redesign-telas-diagrama-artefato.md` e `.agents/tasks/backlog/TASK-009-layout-visao-sistema-artefato.md` antes de codar.
2. TASK-009 é diferente das duas anteriores: Visão do Sistema **não usa o grid canvas/inspector** (é navegação módulo→entidade + detalhe, ver `src/features/system-view/SystemViewPage.tsx` atual) — só reaproveita os tokens de cor/tipografia de `diagram-shell/` (`src/index.css`), não o `DiagramShell`/`useCanvasZoomPan`.
3. Reler o artefato (`Artifact` tool, `action: "read"`, URL acima) focando em `#overview-wrap`/`#ov-nav`/`#ov-detail` e as classes `.ov-*` (já vistas de relance ao ler o CSS nas tasks anteriores, mas não implementadas ainda).
4. Decidir (e registrar a decisão) se `DiagramRouterPage.tsx` também para de envolver a Visão do Sistema em `AppLayout` — hoje é a única das 3 visualizações que ainda usa o layout antigo.
5. Depois de TASK-009: **TASK-010** (modais de import/export estilizados conforme o artefato — depende de TASK-006 e TASK-007, ambas já prontas).

## Arquivos modificados
Ver `git status`/`git diff --stat` no repositório — resumo: `src/index.css` (tokens + ~900 linhas de componentes do shell), `src/features/diagram-shell/` (novo), `src/features/class-diagram/*` (canvas avançado + testes), `src/features/object-diagram/*` (canvas avançado + testes), `src/features/navigation/DiagramRouterPage.tsx`, `src/App.tsx`, `index.html` (fontes), `.agents/context/CONTEXT.md`, `docs/architecture/components.md`, e as 3 tasks movidas para `.agents/tasks/completed/`.

## Comandos executados
- `npm run build` — OK (tsc -b && vite build), sem erros, em cada uma das 3 tasks.
- `npm run lint` — OK (oxlint), só os 4 warnings pré-existentes de outras features, nenhum novo.
- `npm test` — 12 arquivos, **82 testes passando** (44 no início da sessão → 50 após TASK-006 → 74 após TASK-007 → 82 após TASK-008).

## Falhas atuais
Nenhuma. Único ponto de atenção não resolvido (não é falha, é decisão adiada): um bug real de duplo-wrapping em `DiagramShell` foi encontrado e corrigido durante a validação visual da TASK-007 (`canvasProps` foi adicionado por causa disso) — ver "Decisões" na própria TASK-007 para não repetir o erro se a TASK-009 vier a usar `DiagramShell` de algum jeito.

## Decisões tomadas
- Zoom/pan (`canvasTransform.ts`/`useCanvasZoomPan.ts`) ficou compartilhado em `diagram-shell/` desde a TASK-007 (não em `class-diagram/`), especificamente pensando em ser reaproveitado sem refactor — confirmado que funcionou na TASK-008 sem tocar nesses arquivos.
- Diagrama de Classes e Diagrama de Objetos pararam de usar `AppLayout` (são full-bleed com `DiagramShell` como chrome próprio) — decisão tomada task a task em `DiagramRouterPage.tsx`, condicionada a `diagram.type`. Visão do Sistema ainda não foi decidida (ver "Próximos passos" item 4).
- Inspector com variante somente-leitura para `visualizador` (em vez de escondido por completo) nas duas tasks — mostra os dados como texto, sem inputs/botões de ação. Mesmo padrão provavelmente vale para a Visão do Sistema, mas ela já tinha esse comportamento antes (não é escopo da TASK-009 mudar isso, seu foco é só visual/layout).

## Não refazer
- Não reabrir a extração de `useCanvasZoomPan`/`canvasTransform` — já está pronta e testada (`canvasTransform.test.ts`), a Visão do Sistema nem vai usá-la.
- Não recriar tokens de cor/tipografia — já estão completos em `src/index.css` desde a TASK-006 (`--bg`, `--surface`, `--object-accent`, etc., dark/light via `prefers-color-scheme`+`[data-theme]`), incluindo as classes `.ov-*` já podem estar parcialmente lá (conferir antes de reescrever).
- Não repetir a comparação visual das 3 telas já concluídas — só validar a Visão do Sistema (TASK-009) quando chegar nela.

## Documentos relevantes
- `.agents/decisions/ADR-002-redesign-telas-diagrama-artefato.md` — decisão geral, plano de adoção das 5 tasks.
- `.agents/tasks/backlog/TASK-009-layout-visao-sistema-artefato.md` — próxima task.
- `.agents/tasks/backlog/TASK-010-modais-import-export-artefato.md` — depois da TASK-009.
- `.agents/tasks/completed/TASK-006-design-system-shell-diagramas.md`, `TASK-007-canvas-avancado-diagrama-classes.md`, `TASK-008-canvas-avancado-diagrama-objetos.md` — histórico do que já foi decidido/implementado, com "Decisões"/"Pendências" registradas em cada uma.
- `.claude/agents/frontend-diagramas.md` — papel especializado responsável por esta task.
